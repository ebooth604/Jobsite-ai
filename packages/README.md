# packages

Shared libraries consumed by apps and services.

| Package | Contents |
|---|---|
| `shared-types/` | Cross-service TypeScript types and schemas |
| `ui-components/` | Shared dashboard components |

A package earns its place by being used in two places. Anything with one consumer
belongs in that consumer until a second one shows up.
