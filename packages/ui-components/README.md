# packages/ui-components

Shared React components for the dashboard.

Kept separate from `apps/dashboard` only for the things a second surface will
plausibly need. Until that second consumer exists, prefer leaving a component in
the dashboard.

## Status

Not started.
